/* eslint-disable react/prop-types */

const Cart = ({course}) => {
    const {title} = course;
    return (
        <div>
            <h3>{title}</h3>
        </div>
            
        
    );
};

export default Cart;